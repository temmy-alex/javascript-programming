const status = 1;
// ternary operator ideal hanya untuk 2 kondisi
// console.log(status == 1 ? 'Aktif' : 'Tidak Aktif')

if (status == 1) {
    console.log('Aktif');
} else {
    console.log('Tidak Aktif');
}
